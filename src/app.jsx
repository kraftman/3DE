import React, { useState } from 'react';
import { createRoot } from 'react-dom/client';
import { FlowManager } from './FlowManager';

const root = createRoot(document.body);
root.render(<FlowManager />);
